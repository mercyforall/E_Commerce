import React from 'react';
import './Cart.css';

function Cart() {
  return (
    <div className="cart">
      <h2>Your Shopping Cart</h2>
      <p>No items in cart.</p>
    </div>
  );
}

export default Cart;
// This is a simple Cart page component that displays a message when the cart is empty.
// You can expand this component later to include cart items, total price, and checkout functionality.  